version: '3.8'

services:
  db:
    build: .
    container_name: boardgame_postgres
    restart: always
    environment:
      POSTGRES_USER: cjw1645
      POSTGRES_PASSWORD: 1q2w3e4r!
      POSTGRES_DB: boardgame_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: